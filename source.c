#include <stdio.h>

int main(){
    // Spawn GTK+ window
    // Check if user is logged in
    // If not, display login window
        // Give an option to register
    // If yes, display main window
    return 0;
}
